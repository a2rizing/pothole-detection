# Core dependencies
torch>=1.12.0
torchvision>=0.13.0
opencv-python>=4.5.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.11.0
scikit-learn>=1.0.0
albumentations>=1.3.0
tqdm>=4.64.0
Pillow>=9.0.0
scipy>=1.7.0

# ROS dependencies (install via apt on Ubuntu/Debian)
# sudo apt install ros-noetic-desktop-full
# sudo apt install ros-noetic-cv-bridge
# sudo apt install ros-noetic-image-transport
# sudo apt install ros-noetic-sensor-msgs
# sudo apt install ros-noetic-geometry-msgs
# sudo apt install ros-noetic-std-msgs

# GPS and Serial communication
pyserial>=3.5
pynmea2>=1.19.0
gpsd-py3>=0.3.0

# Database
sqlite3  # Usually included with Python

# HTTP requests and mapping visualization
requests>=2.28.0
folium>=0.14.0
geopy>=2.3.0

# Additional Pi-specific packages
RPi.GPIO>=0.7.1
picamera>=1.13

# Configuration handling
PyYAML>=6.0

# Optional: Web interface
flask>=2.2.0
flask-cors>=3.0.0

# Optional: Advanced visualization
plotly>=5.0.0
dash>=2.6.0

# Optional: Model optimization
onnx>=1.12.0
onnxruntime>=1.12.0

# Optional: Logging and monitoring
tensorboard>=2.10.0
wandb>=0.13.0

# Development dependencies
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0
pre-commit>=2.20.0

# Raspberry Pi specific (install via apt)
# sudo apt install python3-picamera
# sudo apt install python3-gpiozero
# sudo apt install python3-rpi.gpio